__pycache__/
*.pyc
.venv/
venv/
node_modules/
.env
*.db
*.sqlite3
.next/
dist/
.expo/
.superpowers/
*.egg-info/

# web client (Phase 3)
web/node_modules/
web/.next/
web/out/
# local copy of the published bundle, synced from ../public via
# web/scripts/sync-bundle.mjs — not committed, same as a CDN fetch target
web/public/data/
VERCEL-SETUP.md

# Playwright MCP tool artifacts (snapshots/console logs from manual testing)
.playwright-mcp/
# Root-level only (the pipeline's published bundle output dir) — anchored
# with a leading slash so it does NOT also match web/public/, which holds
# real committed static assets (manifest.webmanifest, sw.js, icons/).
/public/
LLM-PROVIDERS.md
PIPELINE-CLI.md
IMPLEMENTATION-TRACKER.md
*.jpeg
*.jpg
WhatsApp*
